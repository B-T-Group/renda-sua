table:
  name: user_types
  schema: public
is_enum: true
array_relationships:
  - name: users
    using:
      manual_configuration:
        column_mapping:
          id: user_type_id
        insertion_order: null
        remote_table:
          name: users
          schema: public
