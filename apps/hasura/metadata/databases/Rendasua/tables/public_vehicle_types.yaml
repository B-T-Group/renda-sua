table:
  name: vehicle_types
  schema: public
is_enum: true
array_relationships:
  - name: agents
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: agents
        column_mapping:
          id: vehicle_type_id
