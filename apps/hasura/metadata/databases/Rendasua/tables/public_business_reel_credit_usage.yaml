table:
  name: business_reel_credit_usage
  schema: public
object_relationships:
  - name: business
    using:
      foreign_key_constraint_on: business_id
select_permissions:
  - role: business
    permission:
      columns: "*"
      filter:
        business:
          user_id:
            _eq: X-Hasura-User-Id
