table:
  name: document_types
  schema: public
array_relationships:
  - name: user_uploads
    using:
      manual_configuration:
        column_mapping:
          id: document_type_id
        insertion_order: null
        remote_table:
          name: user_uploads
          schema: public
select_permissions:
  - role: agent
    permission:
      columns:
        - id
        - name
        - description
        - created_at
        - updated_at
      filter: {}
  - role: business
    permission:
      columns:
        - id
        - name
        - description
        - created_at
        - updated_at
      filter: {}
  - role: client
    permission:
      columns:
        - id
        - name
        - description
        - created_at
        - updated_at
      filter: {}
