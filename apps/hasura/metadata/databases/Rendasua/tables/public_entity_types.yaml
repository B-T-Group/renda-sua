table:
  name: entity_types
  schema: public
is_enum: true
array_relationships:
  - name: user_messages
    using:
      manual_configuration:
        column_mapping:
          id: entity_type
        insertion_order: null
        remote_table:
          name: user_messages
          schema: public
select_permissions:
  - role: agent
    permission:
      columns:
        - id
        - comment
      filter: {}
  - role: business
    permission:
      columns:
        - id
        - comment
      filter: {}
  - role: client
    permission:
      columns:
        - id
        - comment
      filter: {}
  - role: user
    permission:
      columns:
        - id
        - comment
      filter: {}
