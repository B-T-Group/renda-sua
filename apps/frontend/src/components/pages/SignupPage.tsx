import React from 'react';
import { SignupWizard } from '../signup/wizard/SignupWizard';

/** Thin page shell — wizard logic lives under components/signup/. */
const SignupPage: React.FC = () => <SignupWizard />;

export default SignupPage;
