table:
  name: user_types
  schema: public
is_enum: true
array_relationships:
  - name: users
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: users
        column_mapping:
          id: user_type_id
